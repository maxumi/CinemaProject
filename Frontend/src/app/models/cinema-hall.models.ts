export interface CinemaHall {
    id: number;
    name: string;
    capacity: number;
  }
  export interface CreateCinemaHallDto {
    name: string;
    capacity: number;
  }
  export interface UpdateCinemaHallDto {
    name: string;
    capacity: number;
  }
  